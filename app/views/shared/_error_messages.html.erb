<% if @user.errors.any? %>
  <p>Form contains <%= pluralize(@user.errors.count, "error") %></p>
  <ul>
    <% @user.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
      <% end %>
  </ul>
<% end %>